<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. This file does not exist in the release source set, so a shipped APK
  cannot pick it up however the build is configured.

  Cleartext is allowed for exactly two hosts: the address the Android emulator uses to reach
  the developer's own machine, and the loopback a device sees over `adb reverse`. Everything
  else — including any real server — still requires TLS, so a debug build cannot silently talk
  to a staging API over plain HTTP either.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
