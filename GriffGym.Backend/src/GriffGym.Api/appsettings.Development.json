{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },

  "//": [
    "Local development only.",
    "",
    "The connection string below points at the docker-compose PostgreSQL. Its password is not a",
    "secret in any meaningful sense: that database is reachable only from this machine and holds",
    "nothing but throwaway data.",
    "",
    "Jwt:SigningKey is deliberately absent, here and everywhere else in the repository. Supply one",
    "before running the API directly:",
    "",
    "  dotnet user-secrets set \"Jwt:SigningKey\" \"$(openssl rand -base64 48)\" --project src/GriffGym.Api",
    "",
    "docker compose already provides its own. Startup validation refuses to boot without a key",
    "rather than falling back to something predictable."
  ],

  "ConnectionStrings": {
    "GriffGym": "Host=localhost;Port=5432;Database=griffgym;Username=griffgym;Password=griffgym"
  },

  "Jwt": {
    "AccessTokenMinutes": 15,
    "RefreshTokenDays": 30
  },

  "GriffGym": {
    "ApplyMigrationsOnStartup": true
  }
}
