<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsTestProject>false</IsTestProject>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\GriffGym.Infrastructure\GriffGym.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Testcontainers.PostgreSql" />
    <!-- Shared helpers, not a test project: extensibility.core gives IAsyncLifetime
         and Assert without demanding an executable with a runner in it. -->
    <PackageReference Include="xunit.v3.extensibility.core" />
  </ItemGroup>

</Project>
